[package]
name = "daemon"
version = "0.1.0"
edition = "2021"
description = "JSON-RPC daemon exposing the Flutter Installer core over stdio"

[[bin]]
name = "daemon"
path = "src/main.rs"

[dependencies]
core = { path = "../core" }
serde_json = "1"
anyhow = "1"